using UnityEngine;

[CreateAssetMenu(fileName = "MapSettings", menuName = "Game/Map Settings")]
public class MapSettings : ScriptableObject
{
    public float MapWide;
    public float StartZPosition;
}
